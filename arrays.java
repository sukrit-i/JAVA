import java.util.Arrays;

public class arrays {

    public static void main(String[] args) {
        // char vowels[] = new char[5];
        // char vowels[] = { 'a', 'i', 'e', 'o', 'u' };
        // a e i o u

        // int startingIndex = 1;
        // int endingIndex = 4;

        // vowels[0] = 'a';
        // vowels[1] = 'e';
        // vowels[2] = 'x';
        // vowels[3] = 'o';
        // vowels[4] = 'u';

        // Arrays.sort(vowels, startingIndex, endingIndex);

        // Arrays.sort(vowels);

        // Arrays.fill(vowels, startingIndex, endingIndex, 'x');

        // char key = 'i';
        // int foundItemIndex = Arrays.binarySearch(vowels, startingIndex, endingIndex,
        // key);// returns the index of the found vowwel

        // System.out.println(Arrays.toString(vowels));

        // System.out.println(foundItemIndex);

        // System.out.println(vowels.length);
        ///////////////////////////////////////////////////////////////////////////////////////////////////

        int numbers[] = { 1, 2, 3, 4, 5 };

        // int startingIndex = 1;
        // int endingIndex = 5;

        // int copyOfNumbers[] = Arrays.copyOf(numbers, numbers.length);
        // int copyOfNumbers[] = Arrays.copyOf(numbers, 10);
        // int copyOfNumbers[] = Arrays.copyOf(numbers, 2);
        // int copyOfNumbers[] = Arrays.copyOfRange(numbers, startingIndex,
        // endingIndex);

        // Arrays.fill(numbers, 0);

        System.out.println(Arrays.toString(numbers));
        // System.out.println(Arrays.toString(copyOfNumbers));

    }
}
